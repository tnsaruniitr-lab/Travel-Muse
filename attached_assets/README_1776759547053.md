# How TRYPS Works — Blog Bundle (V3, audited 100%)

**Slug:** `/blog/how-tryps-works`
**Published:** 2026-04-21
**Format:** How-To / Process Guide
**Audit score:** 100% (V3, all 5 sections clean)

---

## What's in this folder

| File | What it is | Use it for |
|---|---|---|
| `how-tryps-works.html` | Complete self-contained HTML page (602 KB). All 6 schemas in `<head>`, all 5 screenshots base64-embedded, Tailwind via CDN. Opens in any browser. | Browser preview, hand-off to a dev, copy-paste into a static-site project, validate with Google Rich Results Test |
| `how-tryps-works-schemas.json` | All 6 JSON-LD schemas as a clean array, properly indented | Paste individual schemas into your CMS schema-markup field, run through schema.org validator |
| `how-tryps-works-head-meta-and-schemas.html` | Just the `<head>` content — meta tags + Open Graph + Twitter cards + 6 JSON-LD blocks. No Tailwind. | Paste into a CMS "custom head HTML" or "SEO" field |
| `how-tryps-works-body.html` | Just the `<article>` body content — no `<head>`, no `<header>`, no `<footer>` | Paste into a CMS rich-text/HTML editor body |
| `README.md` | This file | Reference |

---

## Schemas included (6 total)

1. **Article** — full entity fields including `@id#article`, `alternativeHeadline`, ImageObject hero, mentions[], keywords[], wordCount, inLanguage, articleSection, about: SoftwareApplication
2. **Organization** — TRYPS publisher entity with logo as ImageObject, founder, sameAs links
3. **BreadcrumbList** — 4 levels (Home > Blog > Product guides > How TRYPS Works)
4. **WebPage** — with `@id#webpage` and speakable selectors (.quick-answer, .summary-box, .faq-a)
5. **HowTo** — totalTime PT15M, estimatedCost $0, 5 steps with #step-N URL anchors mirroring the H2s
6. **FAQPage** — 7 questions, count matches HTML `<details>` count exactly

---

## Before publishing — checklist

- [ ] **Replace base64 images with real CDN URLs.** Find/replace each `data:image/png;base64,...` blob with a real path like `/images/blog/how-tryps-works/step-1-invite.png`. After this swap, the file drops from 602 KB to ~30 KB. The 5 source PNGs are the screenshots you uploaded.
- [ ] **Update the OG/Twitter image URL** at the top of `<head>` — currently points to `https://trypsagent.com/images/blog/how-tryps-works/hero.png`. Set it to your real hero image export.
- [ ] **Confirm publish date** — currently set to `2026-04-21T10:00:00+00:00`. Update `<meta property="article:published_time">`, `Article.datePublished`, `Article.dateModified`, and `WebPage.datePublished/dateModified` if publishing on a different date.
- [ ] **Validate schemas** — paste `how-tryps-works.html` (or each schema from the JSON file) into [Google Rich Results Test](https://search.google.com/test/rich-results) before going live.
- [ ] **Verify breadcrumb path** — the breadcrumb assumes you have a `/blog/product-guides` topic page. If you don't yet, either create it or simplify the breadcrumb to 3 levels (Home > Blog > Post).

---

## Image source files

The 5 screenshots embedded in this blog are App Store-style marketing shots with the following content (in display order in the body):

1. **Hero / Step 3 (repeated)** — Activities tab: Confirmed drop zone, Voting Block with Olive's Mediterranean Grill card, Discover feed
2. **Step 1** — Trip created / iMessage invite screen with "Text Friends" CTA
3. **Step 2** — Vibe questionnaire with 6 preference dimensions
4. **Step 4** — Itinerary tab with day selector, weather forecast, and timeline
5. **Step 5** — Expenses tab with "All Settled" status, Dinner at Shibuya, Train tickets

If you want to swap to clean phone-only renders (without the red gradient marketing background and headline text), generate those exports and drop them at the same paths.
